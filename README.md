
  # AI Recipe Suggestion Website

  This is a code bundle for AI Recipe Suggestion Website. 
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
